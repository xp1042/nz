
# 🚀 从零开始保姆级教程 × Hugging Face 部署哪吒面板 **V2**

> ⚠️ **注意事项**
> 本项目 **需要使用 OAuth 2.0 完成**（旧版 V1 方式已失效）！
> 
> （+内嵌agent，需要）**私有仓库存储备份文件**

---

## 一、前期准备

准备账号，注册都是免费的：

* ✔️ 部署账号（可全自动部署面板）
* ✔️ 一个 Cloudflare Tunnel 隧道域名（不需要暴露真实 IP）
* ✔️ 用于备份存储的 GitHub（私有仓库，数据更安全）
* ✔️ Hugging Face 免费容器***部署完成后***，建议绑定自定义域名

---

## 二、准备基础账号

### 2.1 需要准备的平台

| 平台       | 地址                      | 用途                                       |
|------------|---------------------------|--------------------------------------------|
| GitHub     | https://github.com        | 存放代码、构建镜像、备份数据 |
| Cloudflare | https://dash.cloudflare.com | 域名管理和隧道管理 |
| Hugging Face | https://huggingface.co  | 免费 Docker 容器 |

### 2.2 创建 GitHub 备份仓库

1. 登录 GitHub
2. 右上角点击 **+** → **New repository**
3. 仓库名称，(可以修改，如`nezha-backup`)
4. 勾选 **Private**（私有，保安全）
5. ✔️ 可选 **Add a README file**
6. 点击 **Create repository**

> 🚫 记住自己的 GitHub 用户名和备份仓库的名字

### 2.3 创建 GitHub Token

1. 点击 GitHub 右上角头像 → **Settings**
2. 左侧栏最下 → **Developer settings**
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token** → **Generate new token (classic)**
5. 填写：
   - **Note**：`nezha-backup`
   - **Expiration**：**No expiration**
   - **选择权限**：✔️ `repo`、✔️ `write:packages`、✔️ `read:packages`
6. 点击 **Generate token**
7. **立即复制保存 Token**（只显示一次）

> ⚡ Token 格式：`ghp_xxxxxxxxxxxxxxxxxxxxxxxx`

---

## 三、Fork 本仓库

1. 访问本项目仓库
2. 右上角点击 **Fork**
3. **Owner** 使用你自己的用户名
4. 点击 **Create fork**

---

## 四、构建 Docker 镜像

### 4.1 启用 Actions

1. 打开你 Fork 后的仓库
2. 点击 **Actions**
3. 点击 **I understand my workflows, go ahead and enable them**

### 4.2 运行镜像构建

1. 左侧栏点击 **🐳 构建最新的镜像并上传**
2. 点击 **Run workflow**
3. 输入镜像名（如 `nz`，可以随意）,镜像tag 默认 `latest`
4. 点击绿色 **Run workflow**
5. 等待构建完成（3-10分钟）

### 4.3 获取镜像地址

构建完成后，镜像地址格式：

```
ghcr.io/你的用户名/镜像名:latest
```

---

## 五、创建 Cloudflare Tunnel

### 5.1 创建隧道并获取 Token

1. 登录 [Cloudflare Zero Trust](https://one.dash.cloudflare.com/)
2. **Networks → Tunnels**
3. **Create a tunnel** → 选择 **Cloudflared** → 命名（如 `nezha`）
4. 保存后复制页面上的 **Token**（很长的 `eyJ...` 字符串）→ 即 `ARGO_AUTH`
5. **Public Hostname** 添加：把你的子域名（如 `nezha.example.com`）指向 `HTTP://localhost:8008`

> ⚡ `ARGO_AUTH` 就是这个 Token；`ARGO_DOMAIN` 就是你绑定的子域名

---

## 六、部署 Hugging Face Space

### 6.1 新建 Space

1. 登录 [Hugging Face](https://huggingface.co) → **New Space**
2. **Space SDK** 选择 **Docker** → **Blank**
3. 可见性选择 **Public**（变量放在 Secrets 中不会泄露）
4. 创建 Space

### 6.2 配置环境变量（Secrets）

进入 Space → **Settings → Variables and secrets**，逐个添加：

| 变量名 | 说明 | Secret |
|--------|------|--------|
| `ARGO_AUTH` | 隧道 Token | ✔️ |
| `ARGO_DOMAIN` | 你的面板域名 | ❌ |
| `GITHUB_TOKEN` | 备份用 Token | ✔️ |
| `GITHUB_REPO_OWNER` | 你的 GitHub 用户名 | ❌ |
| `GITHUB_REPO_NAME` | 备份仓库名 | ❌ |
| `ZIP_PASSWORD` | 备份加密口令 | ✔️ |
| `NZ_UUID` | 面板服务器 UUID | ❌ |
| `NZ_CLIENT_SECRET` | 面板 Agent 密钥（可不填，自动生成） | ❌ |

### 6.3 编写 Space 的 README

把 Space 中自动生成的 `README.md` 替换为：

```markdown
---
title: Nz V2
emoji: 🚀
colorFrom: gray
colorTo: gray
sdk: docker
app_port: 7860
---
```

再新建一个 `Dockerfile`（或直接使用 `Dockerfile` 内容引用镜像）：

```
FROM ghcr.io/你的用户名/镜像名:latest
```

保存后 Space 会自动构建，**等待 3-10 分钟**。

---

## 七、初始化面板（V2）

1. 访问你的域名（如 `https://nezha.example.com`）
2. 默认账号：`admin` / 默认密码：`admin` → **立即修改密码！**
3. **管理后台 → 服务器 → 添加服务器**：获得 `UUID`，回填到 Space 的 `NZ_UUID`
4. **管理后台 → 系统设置**：`Agentsecretkey` 可回填到 `NZ_CLIENT_SECRET`（也可不填）
5. 重启 Space，服务器上线即表示内嵌 Agent（V2）工作正常

---

## 八、验证与日常维护

1. **访问面板**：打开你的域名进入 V2 管理后台
2. **数据备份**：每天自动备份；手动触发——把备份私库 `README.md` 内容改为 `backup`
3. **恢复**：重建容器时自动拉取最新备份恢复
4. **（可选）保活**：用你自己的 UptimeRobot 等服务监控面板地址（本项目已移除内置的第三方保活上报，更符合隐私要求）

---

### *教程完！* 🎉
